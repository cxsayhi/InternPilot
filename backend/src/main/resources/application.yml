server:
  port: 8080

spring:
  application:
    name: internpilot-backend

internpilot:
  agent-service:
    base-url: ${AGENT_SERVICE_BASE_URL:http://localhost:8000}

management:
  endpoints:
    web:
      exposure:
        include: health,info

